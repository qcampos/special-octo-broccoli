<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="notification_security.upem.fr.securitynotification">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppThemess">
        <activity
            android:name=".home.HomeActivity"
            android:label="@string/title_activity_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".network.NetworkService"
            android:exported="false">
        </service>

        <activity
            android:name=".map.UrgencyMapActivity"
            android:label="@string/title_activity_urgence_map"></activity>
    </application>

</manifest>
